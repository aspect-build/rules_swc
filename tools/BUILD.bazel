load("@bazelrc-preset.bzl", "bazelrc_preset")

bazelrc_preset(
    name = "preset",
    # The output is specific to the version in .bazelversion
    tags = [
        "skip-on-bazel8",
        "skip-on-bazel9",
    ],
)
